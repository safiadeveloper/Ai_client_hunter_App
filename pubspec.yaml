name: ai_client_hunter
description: "A new Flutter project for Autonomous AI Lead Generation."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI & Icons
  cupertino_icons: ^1.0.8

  # Database & Auth
  supabase_flutter: ^2.8.1

  # AI Engine (Gemini)
  google_generative_ai: ^0.4.0

  # Networking & Scraping
  http: ^1.1.0
  html: ^0.15.4

  # Email Automation (Add this for Sending Emails)
  mailer: ^6.0.1
  enough_mail: ^2.1.6
  
  # Date formatting
  intl: ^0.18.1

  # Background Tasks
  workmanager: ^0.9.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
